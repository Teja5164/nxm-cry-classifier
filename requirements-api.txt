# requirements-api.txt — PRODUCTION API ONLY (used by Docker)
# NOTE: torch is installed separately in the Dockerfile via PyTorch CPU-only index.
# This file covers all other runtime dependencies.
# ─────────────────────────────────────────────────────────────────────────────

# Audio processing
librosa==0.11.0
soundfile==0.13.1

# Numerics
numpy
scipy==1.15.3
scikit-learn==1.6.1

# API server
fastapi==0.115.12
uvicorn[standard]==0.34.0
python-multipart==0.0.20
